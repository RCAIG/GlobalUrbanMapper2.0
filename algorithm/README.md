The code is designed to train a robust model capable of handling both **complete** and **incomplete** modality observations.

To simulate realistic scenarios where certain modalities may be partially missing or corrupted, synthetic masks are provided in the `/generate_masks` directory.
